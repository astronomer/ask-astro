```sh
$ poetry run inv docs.serve -h

Usage: inv[oke] [--core-opts] docs.serve [--options] [other tasks here ...]

Docstring:
  Serve the docs locally (http://127.0.0.1:8000)

Options:
  -r, --rebuild   clean and build the doc before serving
```
