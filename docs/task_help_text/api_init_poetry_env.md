```sh
$ poetry run inv api.run-with-poetry -h

Usage: inv[oke] [--core-opts] api.run-with-poetry [--options] [other tasks here ...]

Docstring:
  Run ask-astro API server with poetry

Options:
  -i, --init   initialize poetry environment before running server
```
