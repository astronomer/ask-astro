```sh
$ poetry run inv ui.run -h

Usage: inv[oke] [--core-opts] ui.run [--options] [other tasks here ...]

Docstring:
  Run UI server

Options:
  -i, --init           init UI dev env
  -o, --open-browser   open the browser after running the server
```
