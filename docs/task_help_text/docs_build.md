```sh
$ poetry run inv docs.build -h

Usage: inv[oke] [--core-opts] docs.build [--options] [other tasks here ...]

Docstring:
  Build sphinx docs

Options:
  -c, --clean   clean the docs before building
```
